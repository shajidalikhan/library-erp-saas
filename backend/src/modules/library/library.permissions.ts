/**
 * Module-local permission references — always delegate to the central catalog.
 */
import { PERMISSIONS } from '@constants/permissions.constants';

export const LIBRARY_PERMISSIONS = {
  LIBRARY_CREATE: PERMISSIONS.LIBRARY_CREATE,
  LIBRARY_READ: PERMISSIONS.LIBRARY_READ,
  LIBRARY_UPDATE: PERMISSIONS.LIBRARY_UPDATE,
  LIBRARY_DELETE: PERMISSIONS.LIBRARY_DELETE,
  BRANCH_CREATE: PERMISSIONS.BRANCH_CREATE,
  BRANCH_READ: PERMISSIONS.BRANCH_READ,
  BRANCH_UPDATE: PERMISSIONS.BRANCH_UPDATE,
  BRANCH_DELETE: PERMISSIONS.BRANCH_DELETE,
} as const;
